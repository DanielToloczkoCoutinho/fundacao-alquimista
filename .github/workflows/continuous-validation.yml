# .github/workflows/continuous-validation.yml
name: Ciclo de Validação Contínua & Deploy Cósmico

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  static-analysis:
    name: Análise Estática e Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      - name: Instalar Dependências
        run: npm ci
      - name: Rodar Linter
        run: npm run lint
      - name: Verificação de Tipos (TypeScript)
        run: npm run typecheck

  cosmic-tests:
    name: Testes Cósmicos (Unit, E2E, Security)
    needs: static-analysis
    runs-on: ubuntu-latest
    services:
      fundacao-omega-backend:
        image: node:18
        ports:
          - 3000:3000
        options: --name fundacao-omega-backend
        volumes:
          - ${{ github.workspace }}:/app
        working-dir: /app
        command: sh -c "npm ci && node backend/server.js"
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      - name: Instalar Dependências
        run: npm ci
      - name: Rodar Testes Unitários
        run: npm run test:unit
      - name: Rodar Testes E2E
        run: npm run test:e2e
        env:
          PLAYWRIGHT_TEST_BASE_URL: http://localhost:3000
      - name: Scan de Segurança
        run: npm run security-scan

  chaos-experiment:
    name: "🌪️ Experimento de Caos — Resiliência Ω"
    runs-on: ubuntu-latest
    needs: cosmic-tests
    steps:
      - name: Checkout do Códice
        uses: actions/checkout@v3

      - name: Injetar caos de rede
        run: |
          echo "Simulando a injeção de caos. Em um ambiente real, este passo teria acesso ao container do backend."
          echo "🔧 Injetando 200ms de delay + 5% packet loss"
          sleep 5
          echo "♻️ Revertendo o caos"
          
      - name: Validar auto-cura
        run: |
          echo "🔍 Verificando health após caos"
          echo "Simulando verificação. Em um ambiente real, faria uma requisição ao health check."
          # curl --fail --retry 5 --retry-delay 5 http://localhost:3000/health

  cosmic-deploy:
    name: Deploy Canário no Reino Cósmico
    needs: chaos-experiment
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      - name: Instalar Dependências
        run: npm ci
      - name: Build da Aplicação Cósmica
        run: npm run build
        env:
          NEXT_PUBLIC_CDN_ENABLED: true
          NEXT_PUBLIC_CDN_DOMAIN: ${{ secrets.CDN_DOMAIN }}
      - name: Deploy com Argo Rollouts
        run: |
          echo "Aplicando rollout canário para a nova imagem..."
          # Em um cenário real, aqui entraria o comando para atualizar a imagem no Kubernetes
          # kubectl argo rollouts set image rollout/foundation-backend backend=foundation-backend:${{ github.sha }}
          echo "Deploy canário iniciado com Argo Rollouts."
      - name: Verificar Rollout
        run: |
          # Em um cenário real, monitoraria o status do rollout
          # kubectl argo rollouts get rollout foundation-backend --watch
          echo "Rollout concluído com sucesso."

  cosmic-cdn-purge:
    name: Purge do Cache da CDN
    needs: cosmic-deploy
    runs-on: ubuntu-latest
    steps:
      - name: Purge do Cache Cloudflare
        uses: jakejarvis/cloudflare-purge@v0.6.0
        with:
          zone: ${{ secrets.CLOUDFLARE_ZONE }}
          purgeEverything: true
        env:
          CLOUDFLARE_TOKEN: ${{ secrets.CLOUDFLARE_TOKEN }}
